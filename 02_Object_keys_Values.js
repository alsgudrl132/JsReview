const user = {
    name: 'Min',
    age: 15,
    gender: 'male',
};
console.log(Object.keys(user)); //[ 'name', 'age', 'gender' ] 키 값만 가져옴
console.log(Object.values(user)); //[ 'Min', 15, 'male' ] 벨류 값만 가져옴
